name = "3d-export-dispatch-proxy"
main = "dispatch-proxy.js"
compatibility_date = "2024-11-01"

# Created with: wrangler kv namespace create RATE_LIMIT_KV
# Then paste the returned id below.
kv_namespaces = [
  { binding = "RATE_LIMIT_KV", id = "f982058def9c4ab9aa6dd5304b78328d" }
]

# Set with: wrangler secret put GITHUB_TOKEN
# (a fine-grained PAT scoped to just this repo, Actions: Read and write)
